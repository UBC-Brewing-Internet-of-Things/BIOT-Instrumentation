{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Attach to Chrome",
			"port": 3000,
			"request": "attach",
			"type": "chrome",
			"webRoot": "${workspaceFolder}"
		},
		{
			"type": "node",
			"name": "startttt",
			"request": "launch",
			"command": "npm start",
			"cwd": "${workspaceFolder}"
		}
	]
}